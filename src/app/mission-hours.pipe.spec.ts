import { MissionHoursPipe } from './mission-hours.pipe';

describe('MissionHoursPipe', () => {
  it('create an instance', () => {
    const pipe = new MissionHoursPipe();
    expect(pipe).toBeTruthy();
  });
});
